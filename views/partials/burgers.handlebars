{{#if devoured}}

<div class="row">
    <p>{{burger_name}}</p>
        <button class="button remove" data-id={{id}}>Remove from list</button>
</div>

{{else}}

<div class="row">
    <p>{{burger_name}}</p>
        <button class="button devour" data-id={{id}}>Devour!</button>
</div>

{{/if}}