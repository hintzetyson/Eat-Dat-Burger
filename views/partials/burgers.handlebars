{{#if devoured}}

<div class="row">
    <p>{{burger_name}}</p>
        <button class="button" data-id={{id}}>Remove from list</button>
</div>

{{else}}

<div class="row">
    <p>{{burger_name}}</p>
        <button class="button" data-id={{id}}>Devour!</button>
</div>

{{/if}}